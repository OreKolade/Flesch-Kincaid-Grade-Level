
        ###  Find A way to write into a file from the internet in a way you way : i.e line by line 

"""
## This just prints out content from the webpage into the IDE

from urllib.request import urlopen

data = urlopen("http://svn.code.sf.net/p/cmusphinx/code/trunk/cmudict/cmudict-0.7b") 
for line in data: 
    print (line)
"""


"""
## Using this code to write from the internet
## will not necessaryly print the text in the desired form.
## Will also make a new copy of the text 
##It makes every thing compressed together.
## I want to print it out line for line from the internet to a text doc.

import urllib.request
urllib.request.urlretrieve("http://svn.code.sf.net/p/cmusphinx/code/trunk/cmudict/cmudict-0.7b", "//SRVA/Homes$/koladeor/Desktop/Python Folder/Word Decomposition into Phones.txt")

"""


from urllib.request import urlopen

data = urlopen("http://svn.code.sf.net/p/cmusphinx/code/trunk/cmudict/cmudict-0.7b")
new_file= open("Word Decomposition into Phones.txt","w")

for line in data:
    if str(line)[2:5] == ";;;" :##Starts printing at a specified point 
        continue
    else:
        new_line = str(line).replace(str(line)[0:2],"")#For some reason, whenever I write to a file ,
        new_file.write(new_line + '\n')                #" b' " is written beside every line. This code
                          #writes the entire file content                                                                                   #and removes the unsigthly notation


                                            ### Next thing will be to write every line after a specifc point into a Dictionary
                                     
                                        





